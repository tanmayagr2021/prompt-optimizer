# Core UI
streamlit>=1.32.0

# File extraction
markitdown[all]>=0.0.1

# NLP — intent analysis (rule-based fallback)
spacy>=3.7.0
nltk>=3.8.0

# AI backend — local LLM via Ollama (optional but strongly recommended)
# Install Ollama from https://ollama.com/download, then: ollama pull llama3.2
ollama>=0.4.0

# Semantic redundancy detection (optional)
# pip install torch --index-url https://download.pytorch.org/whl/cpu
# pip install sentence-transformers
sentence-transformers>=2.5.0
